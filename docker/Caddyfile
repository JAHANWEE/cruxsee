# Cruxsee — Caddy reverse proxy
# Handles auto-SSL via Let's Encrypt

cruxsee.in, www.cruxsee.in, home.cruxsee.in, chat.cruxsee.in {
	reverse_proxy web:3000
}

api.cruxsee.in {
	reverse_proxy api:4000
}
